# Legacy Compatibility Wrappers

The recommended experiment entrypoints now live under:

- `experiments/comparison/`
- `experiments/hyperparameter_analysis/`
- `experiments/sampling_bias/`

This `tools/` directory is kept as a compatibility layer so older commands continue to work.
