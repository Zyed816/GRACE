# 历史目录说明

该目录用于存放重构前 `tools/` 目录的历史兼容信息。

当前项目的正式入口已经统一迁移到：

- `experiments/method_comparison/`
- `experiments/hyperparameter_sensitivity/`
- `experiments/sampling_bias_validation/`

如果你在旧笔记、旧命令或旧论文附录中看到 `tools/...` 形式的路径，请直接改用根目录 [README.md](../README.md) 中给出的新命令。
