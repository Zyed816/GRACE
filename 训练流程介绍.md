# GRACE 训练流程介绍

本文档基于 `train.py` 与 `model.py` 中的源码，对 GRACE-IFL 方法的完整训练流程进行逐阶段讲解。整个流程可归纳为五个核心步骤：**预热训练** → **相似度计算** → **隐藏正样本挖掘** → **样本集更新** → **修正损失函数优化**。

---

## 1. 预热训练（Warmup）

### 文件：`train.py`
### 函数：
- `train_grace()` — 第 388–405 行
- `train_gca()` — 第 675–706 行
- 主循环调度逻辑 — 第 872–1008 行

### 机制说明

在训练初期（epoch ≤ `warmup_epochs`），模型不引入任何隐藏正样本，而是使用基础的图对比学习目标进行预热。预热阶段的目的在于让编码器先学到具有一定判别力的节点表示，为后续的高质量相似度计算奠定基础。

根据所选的图增强策略，预热函数有所不同：

- **SG-GR 方法**：调用 `train_grace()`（第 965 行），采用均匀随机边丢弃 + 特征丢弃生成两个增强视图，使用标准 InfoNCE 损失进行训练。
- **SG-GC 方法**：调用 `train_gca()`（第 909 行），采用 GCA（Graph Contrastive Augmentation）中基于度的结构感知丢弃策略，根据边中心性的重要性加权进行边丢弃和特征丢弃，同样使用标准 InfoNCE 损失。

**核心代码路径**（`train.py` 主循环，第 960–973 行）：

```python
# SG-GR warmup
if epoch <= warmup_epochs:
    loss, exp1_stats = train_grace(model, data.x, data.edge_index,
                                   contrastive_batch_size=contrastive_batch_size, ...)
    phase = 'warmup'
```

```python
# SG-GC warmup
if epoch <= warmup_epochs:
    loss, exp1_stats = train_gca(model, data.x, data.edge_index,
                                 gca_drop_scheme, gca_drop_weights,
                                 gca_feature_weights,
                                 contrastive_batch_size=contrastive_batch_size, ...)
    phase = 'warmup-gca'
```

---

## 2. 相似度计算（Similarity Calculation）

### 文件：`model.py`
### 函数：
- `Model.sim()` — 第 66–69 行
- `Model._similarity_terms()` — 第 71–77 行

### 机制说明

相似度计算是整个 IFL 流程的基础。GRACE 使用**余弦相似度**来衡量节点嵌入之间的语义近邻关系。

#### `Model.sim()` — 余弦相似度矩阵

```python
def sim(self, z1: torch.Tensor, z2: torch.Tensor):
    z1 = F.normalize(z1)   # L2 归一化
    z2 = F.normalize(z2)   # L2 归一化
    return torch.mm(z1, z2.t())  # 矩阵乘法得到 N×N 相似度矩阵
```

两视图嵌入分别经过 L2 归一化后做内积，得到成对余弦相似度矩阵。

#### `Model._similarity_terms()` — 损失函数所需三项

```python
def _similarity_terms(self, z1, z2):
    f = lambda x: torch.exp(x / self.tau)  # 温度参数 τ 缩放后取指数
    refl_sim = f(self.sim(z1, z1))          # 自视图相似度（分母中的负样本项）
    between_sim = f(self.sim(z1, z2))       # 跨视图相似度（正样本来源）
    denom = refl_sim.sum(1) + between_sim.sum(1) - refl_sim.diag()  # 分母（排除自身）
    return refl_sim, between_sim, denom
```

此函数为 InfoNCE 损失的计算准备三项核心量：
- `refl_sim`：同一视图内的自相似度，用于构造负样本项
- `between_sim`：跨视图相似度，正样本从此矩阵中提取
- `denom`：每个节点的归一化分母（所有负样本相似度之和）

---

## 3. 隐藏正样本挖掘（D_U^+ Mining）

### 文件：`train.py`
### 函数：`mine_unlabeled_positives()` — 第 407–581 行

### 机制说明

这是 IFL 方法的核心创新之一。在预热阶段结束后，模型在**无增强的干净图**上计算节点嵌入，并基于嵌入之间的余弦相似度挖掘语义上相近但拓扑上可能不相连的节点对，作为"隐藏正样本"（记为 D_U^+）。

#### 挖掘流程（四步）：

**Step 1 — 计算干净嵌入**（第 418–420 行）：
```python
model.eval()
with torch.no_grad():
    z = model(x, edge_index)      # 干净图上的嵌入
    z = F.normalize(z)            # L2 归一化
```

**Step 2 — 计算 N×N 相似度矩阵并确定阈值**（第 423–434 行）：
```python
sim = torch.mm(z, z.t())         # 全节点相似度矩阵
offdiag = sim[~eye_mask]         # 提取非对角线元素
if similarity_threshold is None:
    active_threshold = percentile_threshold(offdiag, similarity_percentile)  # 百分位阈值
else:
    active_threshold = torch.tensor(float(similarity_threshold))  # 固定阈值
```

相似度超过阈值（默认取 99.5 百分位或固定值 0.8）的节点对被初步标记为正样本候选。

**Step 3 — Top-K 筛选与双向互选**（第 437–446 行）：
```python
if max_du_per_node > 0:                      # 每个节点最多保留 K 个候选
    topk_mask = ...                           # 取 top-k 相似度的节点
    du_pos_mask = du_pos_mask & topk_mask

if use_mutual_topk:                           # 双向互选：只保留双向都在对方 top-k 中的对
    du_pos_mask = du_pos_mask & du_pos_mask.t()
```

**Step 4 — 指数权重计算**（第 447–451 行）：
```python
sim_norm = (sim - sim_min) / (sim_max - sim_min + 1e-12)  # 归一化到 [0, 1]
du_pos_weight = torch.exp(beta * sim_norm) * du_pos_mask.float()  # 指数加权
```

相似度越高的节点对获得越大权重（β 默认 2.0），使模型更关注高置信度的语义正样本。

#### 大规模数据集的分块挖掘

对于 PubMed / DBLP 等大数据集，函数提供 `mining_batch_size` 分块路径（第 469–581 行），避免构造 N×N 稠密矩阵导致 OOM（显存溢出）。分块路径还额外构建了 CSR 稀疏格式的 D_U^+ 权重矩阵，用于后续的稀疏损失计算。

---

## 4. 样本集更新（D_U^+ Refresh / Update Interval）

### 文件：`train.py`
### 函数：主循环调度逻辑 — 第 872–1008 行

### 机制说明

隐藏正样本集 D_U^+ 并非每个 epoch 都重新挖掘，而是**按固定间隔周期性更新**。这是因为：
1. 每次挖掘需要在前向传播中计算全图 N×N 相似度，开销较大；
2. 模型参数在相邻几个 epoch 内变化缓慢，频繁更新 D_U^+ 收益有限。

更新逻辑通过 `update_interval` 参数控制（默认 10 个 epoch）：

**SG-GR 路径**（第 975–976 行）：
```python
if du_cache is None or (epoch - warmup_epochs - 1) % update_interval == 0:
    du_cache = mine_unlabeled_positives(model, data.x, data.edge_index,
                                        similarity_threshold, similarity_percentile,
                                        max_du_per_node, use_mutual_topk, beta,
                                        mining_batch_size=mining_batch_size)
    refresh_du = True
```

**SG-GC 路径**（第 922–923 行）：
```python
if du_cache is None or (epoch - warmup_epochs - 1) % update_interval == 0:
    du_cache = mine_unlabeled_positives(model, ...)  # 同上
    refresh_du = True
```

### 未标记权重渐进调度

同时，修正损失中的未标记权重 λ_u 并非从一开始就全量生效，而是通过**线性渐进升温**策略逐步引入（第 940–941 行 / 第 993–994 行）：

```python
progress = max(epoch - warmup_epochs, 0) / max(corrected_ramp_epochs, 1)
current_unlabeled_weight = unlabeled_weight * min(progress, 1.0)
```

当 `progress < 1.0` 时，λ_u 线性增长；当 `progress ≥ 1.0` 时，达到满值 `unlabeled_weight`。此机制可防止训练早期不稳定的嵌入导致错误的正样本挖掘。

---

## 5. 修正后的损失函数（Corrected InfoNCE Loss）

### 文件：`model.py`
### 函数：
- `Model.corrected_semi_loss()` — 第 92–113 行（SG-GR 变体）
- `Model.corrected_semi_loss_sggc()` — 第 115–145 行（SG-GC 变体）
- `Model.loss()` — 第 370–516 行（总调度函数）
- 批处理版本：`batched_corrected_semi_loss()`（第 147–185 行）、`batched_corrected_semi_loss_sggc()`（第 187–234 行）
- 稀疏版本：`batched_corrected_semi_loss_sparse()`（第 236–281 行）、`batched_corrected_semi_loss_sggc_sparse()`（第 283–338 行）

### 机制说明

修正后的 InfoNCE 损失是 IFL 区别于标准 GRACE 的关键。它扩展了标准 InfoNCE 的正样本定义——不仅包含来自数据增强的原始正样本对（D_L^+，即同一节点在两个增强视图中的嵌入），还额外引入了从语义空间挖掘的隐藏正样本（D_U^+）。

### 5.1 损失函数统一形式

修正后的损失函数可写为：

$$\mathcal{L} = \mathcal{L}_{D_L^+} + \lambda_u \cdot \mathcal{L}_{D_U^+}$$

其中：
- $\mathcal{L}_{D_L^+}$ = 标准 InfoNCE 损失（对角线正样本）
- $\mathcal{L}_{D_U^+}$ = 语义引导的未标记正样本损失
- $\lambda_u$ = 未标记权重（渐进升温）

### 5.2 SG-GR 变体：`corrected_semi_loss()`

**位置**：`model.py` 第 92–113 行

```python
def corrected_semi_loss(self, z1, z2, du_pos_mask, du_pos_weight, unlabeled_weight=1.0):
    _, between_sim, denom = self._similarity_terms(z1, z2)

    # D_L^+: 对角线正样本（自身跨视图）
    dl_loss = -torch.log(between_sim.diag().clamp_min(1e-15) / denom)

    # D_U^+: 挖掘的语义正样本
    prob_matrix = between_sim / denom.unsqueeze(1)
    weighted_nll = du_pos_weight * (-torch.log(prob_matrix.clamp_min(1e-15)))
    du_loss = weighted_nll.sum(1) / du_weight_sum  # 每节点取平均

    return dl_loss + unlabeled_weight * du_loss
```

**核心思想**：对于 D_U^+ 中的每个语义正样本对 (i, j)，将其跨视图相似度 `between_sim[i, j]` 也纳入分子（正样本项），以指数权重 `du_pos_weight[i, j]` 调节其重要性。分母保持 InfoNCE 的标准分母不变。

### 5.3 SG-GC 变体：`corrected_semi_loss_sggc()`

**位置**：`model.py` 第 115–145 行

SG-GC 变体进一步扩展了 D_U^+ 的贡献来源——不仅从**跨视图相似度** (`between_sim`) 中提取，还从**自视图相似度** (`refl_sim`) 中提取：

```python
def corrected_semi_loss_sggc(self, z1, z2, du_pos_mask, du_pos_weight,
                               unlabeled_weight=1.0, refl_du_weight=0.3):
    refl_sim, between_sim, denom = self._similarity_terms(z1, z2)

    # D_L^+: 对角线正样本
    dl_loss = -torch.log(between_sim.diag().clamp_min(1e-15) / denom)

    # D_U^+: 来自跨视图和自视图的双通道贡献
    prob_between = (between_sim / denom.unsqueeze(1)).clamp_min(1e-15)
    prob_refl = (refl_sim / denom.unsqueeze(1)).clamp_min(1e-15)

    du_loss = (1.0 - refl_du_weight) * du_loss_between + refl_du_weight * du_loss_refl

    return dl_loss + unlabeled_weight * du_loss
```

**双通道设计动机**：`refl_sim`（自视图相似度）反映同一增强视图内的语义结构，将其纳入正样本可以进一步利用同视图中的语义聚类信号。`refl_du_weight`（默认 0.3）控制自视图项的贡献比例。

### 5.4 总调度函数：`Model.loss()`

**位置**：`model.py` 第 370–516 行

`loss()` 是所有损失计算的统一入口。其调度逻辑为：

1. 将原始嵌入 z1、z2 通过投影头 `projection()` 映射到隐空间得到 h1、h2
2. 若 `corrected=True`：
   - 若 `batch_size=0` 且无 CSR 格式 → 调用 `corrected_semi_loss()` 或 `corrected_semi_loss_sggc()` 的全量版本
   - 若有 CSR 稀疏格式 → 调用稀疏批处理版本
   - 若有 batch_size → 调用稠密批处理版本
3. 若 `corrected=False` → 调用标准 `semi_loss()`（即原始 GRACE 的 InfoNCE 损失）
4. 对两个视图方向的损失取平均：`ret = (l1 + l2) * 0.5`

### 5.5 批处理与稀疏优化

为处理大规模图（PubMed 含 19,717 节点，DBLP 含 17,716 节点），提供了以下优化变体：

| 函数 | 适用方法 | 内存复杂度 | 说明 |
|------|---------|-----------|------|
| `batched_corrected_semi_loss()` | SG-GR | O(BN) | 按行分块计算稠密 D_U^+ 权重 |
| `batched_corrected_semi_loss_sggc()` | SG-GC | O(BN) | 同上，双通道版本 |
| `batched_corrected_semi_loss_sparse()` | SG-GR | O(BN+BK) | 使用 CSR 稀疏格式存储 D_U^+ 权重 |
| `batched_corrected_semi_loss_sggc_sparse()` | SG-GC | O(BN+BK) | 同上，双通道版本 |

---

## 完整训练流程总览

以下以 **SG-GR** 方法为例，按时间顺序总结完整训练流程：

```
┌─────────────────────────────────────────────────────────────────┐
│                      Epoch 1 ~ warmup_epochs                     │
│                                                                  │
│  ┌──────────────────┐    ┌──────────────────┐                   │
│  │  增强视图 1       │    │  增强视图 2       │                   │
│  │  (边丢弃+特征丢弃) │    │  (边丢弃+特征丢弃) │                   │
│  └────────┬─────────┘    └────────┬─────────┘                   │
│           │                       │                              │
│           └───────┬───────────────┘                              │
│                   ▼                                              │
│          ┌────────────────┐                                     │
│          │  Encoder (GCN) │                                     │
│          └───────┬────────┘                                     │
│                  ▼                                               │
│         ┌─────────────────┐                                     │
│         │  standard        │   ← train_grace() / train_gca()     │
│         │  InfoNCE loss   │                                     │
│         └─────────────────┘                                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│            Epoch warmup_epochs+1 ~ num_epochs                    │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ Step 1: 相似度计算 & D_U^+ 挖掘                        │       │
│  │   model.eval() + 干净图前向 → 余弦相似度矩阵             │       │
│  │   → 百分位阈值过滤 → Top-K 互选 → 指数权重              │       │
│  │   → mine_unlabeled_positives()                        │       │
│  └──────────────────────────┬───────────────────────────┘       │
│                             │                                    │
│                             ▼ (每 update_interval 个 epoch)      │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ Step 2: D_U^+ 缓存更新                                 │       │
│  │   du_cache = {du_pos_mask, du_pos_weight, ...}        │       │
│  └──────────────────────────┬───────────────────────────┘       │
│                             │                                    │
│                             ▼                                    │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ Step 3: 修正 InfoNCE 损失优化                           │       │
│  │   L = L_{D_L^+} + λ_u · L_{D_U^+}                    │       │
│  │   → corrected_semi_loss() / corrected_semi_loss_sggc()│      │
│  └──────────────────────────────────────────────────────┘       │
│                                                                  │
│  λ_u 渐进升温: 0 → unlabeled_weight (经 corrected_ramp_epochs)    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 关键配置参数速查

| 参数 | 默认值 | 说明 | 所在位置 |
|------|--------|------|----------|
| `warmup_epochs` | 0 | 预热训练轮数 | `train.py` 第 754 行 |
| `update_interval` | 10 | D_U^+ 更新间隔 | `train.py` 第 755 行 |
| `similarity_threshold` | 0.8 | 相似度固定阈值 | `train.py` 第 756 行 |
| `similarity_percentile` | 99.5 | 相似度百分位阈值 | `train.py` 第 757 行 |
| `max_du_per_node` | 10 | 每节点最多正样本数 | `train.py` 第 758 行 |
| `use_mutual_topk` | True | 是否使用双向互选 | `train.py` 第 759 行 |
| `beta` | 2.0 | D_U^+ 指数权重温度 | `train.py` 第 760 行 |
| `unlabeled_weight` | 1.0 | λ_u 最终值 | `train.py` 第 761 行 |
| `corrected_ramp_epochs` | 50 | λ_u 线性升温轮数 | `train.py` 第 763 行 |
| `refl_du_weight` | 0.3 | SG-GC 自视图项权重 | `train.py` 第 768 行 |

---

## 文件—函数对照总表

| 流程步骤 | 核心文件 | 核心函数 | 行号 |
|----------|----------|----------|------|
| 预热训练 | `train.py` | `train_grace()` | 388–405 |
| 预热训练 | `train.py` | `train_gca()` | 675–706 |
| 预热训练 | `train.py` | 主循环（`phase='warmup'/'warmup-gca'`） | 960–973 / 909–920 |
| 相似度计算 | `model.py` | `Model.sim()` | 66–69 |
| 相似度计算 | `model.py` | `Model._similarity_terms()` | 71–77 |
| 隐藏正样本挖掘 | `train.py` | `mine_unlabeled_positives()` | 407–581 |
| 样本集更新 | `train.py` | 主循环（`refresh_du` 逻辑） | 922–986 |
| 修正损失（SG-GR） | `model.py` | `Model.corrected_semi_loss()` | 92–113 |
| 修正损失（SG-GC） | `model.py` | `Model.corrected_semi_loss_sggc()` | 115–145 |
| 修正损失（总调度） | `model.py` | `Model.loss()` | 370–516 |
| 修正损失（GR 批处理） | `model.py` | `Model.batched_corrected_semi_loss()` | 147–185 |
| 修正损失（GC 批处理） | `model.py` | `Model.batched_corrected_semi_loss_sggc()` | 187–234 |
| 修正损失（GR 稀疏） | `model.py` | `Model.batched_corrected_semi_loss_sparse()` | 236–281 |
| 修正损失（GC 稀疏） | `model.py` | `Model.batched_corrected_semi_loss_sggc_sparse()` | 283–338 |
| 最终评估 | `train.py` | `test()` | 709–712 |
| 最终评估 | `eval.py` | `label_classification()` | 69–83 |
